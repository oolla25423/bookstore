# 📚 Руководство по тестированию API через Swagger UI

## 🚀 Быстрый старт

1. Запустите проект:
   ```bash
   docker-compose up --build
   docker-compose exec web python manage.py migrate
   docker-compose exec web python manage.py shell < create_test_data.py
   ```

2. Откройте Swagger UI:
   **http://localhost:8000/api/schema/swagger-ui/**

---

## 📖 Содержание

- [Регистрация и авторизация](#регистрация-и-авторизация)
- [Работа с книгами](#работа-с-книгами)
- [Работа с авторами](#работа-с-авторами)
- [Создание заказа](#создание-заказа)
- [Отзывы](#отзывы)
- [Экспорт данных](#экспорт-данных-только-для-администраторов)

---

## 🔐 Регистрация и авторизация

### Шаг 1: Регистрация нового пользователя

1. Найдите эндпоинт **POST /api/register/**
2. Нажмите "Try it out"
3. Введите данные:
   ```json
   {
     "username": "testuser",
     "email": "test@example.com",
     "first_name": "Тест",
     "last_name": "Тестов",
     "password": "testpass123",
     "password_confirm": "testpass123"
   }
   ```
4. Нажмите "Execute"
5. В ответе вы получите:
   - Данные пользователя
   - `access` токен (JWT)
   - `refresh` токен

**Скопируйте `access` токен!** Он понадобится для авторизации.

### Шаг 2: Вход существующего пользователя

1. Найдите эндпоинт **POST /api/login/**
2. Нажмите "Try it out"
3. Введите учетные данные:
   ```json
   {
     "username": "user1",
     "password": "user1234"
   }
   ```
   ИЛИ (если создали тестовые данные):
   ```json
   {
     "username": "admin",
     "password": "admin123"
   }
   ```
4. Нажмите "Execute"
5. Скопируйте `access` токен из ответа

### Шаг 3: Авторизация в Swagger

1. **Найдите кнопку "Authorize" в правом верхнем углу страницы** (иконка замка)
2. Нажмите на неё
3. В поле введите: `Bearer <ваш_access_токен>`
   
   Например:
   ```
   Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ0b2tlbl90eXBl...
   ```
   
   ⚠️ **ВАЖНО:** Не забудьте слово `Bearer` и пробел перед токеном!

4. Нажмите "Authorize"
5. Нажмите "Close"

✅ Теперь все ваши запросы будут авторизованными!

---

## 📚 Работа с книгами

### GET /api/books/ - Получить список книг

**Не требует авторизации**

1. Найдите эндпоинт **GET /api/books/**
2. Нажмите "Try it out"
3. Можно добавить параметры:
   - `page` - номер страницы (например, 1)
   - `search` - поиск (например, "война")
   - `author` - фильтр по ID автора (например, 1)
   - `ordering` - сортировка (например, "price" или "-price")
4. Нажмите "Execute"

**Пример с поиском:**
- `search`: война

**Пример с сортировкой:**
- `ordering`: -price (сортировка по цене от большей к меньшей)

### GET /api/books/{id}/ - Детали книги

**Не требует авторизации**

1. Найдите эндпоинт **GET /api/books/{id}/**
2. Нажмите "Try it out"
3. Введите ID книги (например, 1)
4. Нажмите "Execute"

### POST /api/books/ - Создать книгу

**Требует авторизации**

1. Убедитесь, что вы авторизованы (см. [Шаг 3](#шаг-3-авторизация-в-swagger))
2. Найдите эндпоинт **POST /api/books/**
3. Нажмите "Try it out"
4. Введите данные:
   ```json
   {
     "title": "Новая книга",
     "author_id": 1,
     "price": 999.99,
     "description": "Описание новой книги",
     "stock": 10
   }
   ```
5. Нажмите "Execute"

---

## 👨‍💼 Работа с авторами

### GET /api/authors/ - Получить список авторов

**Не требует авторизации**

1. Найдите эндпоинт **GET /api/authors/**
2. Нажмите "Try it out"
3. Можно указать `page` (номер страницы)
4. Нажмите "Execute"

### POST /api/authors/ - Создать автора

**Требует авторизации**

1. Убедитесь, что вы авторизованы
2. Найдите эндпоинт **POST /api/authors/**
3. Нажмите "Try it out"
4. Введите данные:
   ```json
   {
     "name": "Новый Автор",
     "bio": "Биография автора"
   }
   ```
5. Нажмите "Execute"

---

## 🛒 Создание заказа

### POST /api/create-order/ - Оформить заказ

**Требует авторизации**

1. Убедитесь, что вы авторизованы
2. Найдите эндпоинт **POST /api/create-order/**
3. Нажмите "Try it out"
4. Введите данные о товарах:
   ```json
   {
     "items": [
       {
         "book_id": 1,
         "quantity": 2
       },
       {
         "book_id": 3,
         "quantity": 1
       }
     ]
   }
   ```
5. Нажмите "Execute"

**Что произойдет:**
- Создастся новый заказ
- Количество книг на складе уменьшится
- Рассчитается общая стоимость
- Заказ привяжется к вашему пользователю

### GET /api/orders/ - Получить мои заказы

**Требует авторизации**

1. Убедитесь, что вы авторизованы
2. Найдите эндпоинт **GET /api/orders/**
3. Нажмите "Try it out"
4. Нажмите "Execute"

Вы увидите список своих заказов с деталями.

### GET /api/orders/{id}/ - Детали заказа

**Требует авторизации**

1. Найдите эндпоинт **GET /api/orders/{id}/**
2. Нажмите "Try it out"
3. Введите ID заказа
4. Нажмите "Execute"

---

## ⭐ Отзывы

### GET /api/reviews/ - Получить отзывы

**Не требует авторизации**

1. Найдите эндпоинт **GET /api/reviews/**
2. Нажмите "Try it out"
3. Можно добавить фильтр:
   - `book` - ID книги (например, 1)
   - `rating` - оценка (1-5)
4. Нажмите "Execute"

**Пример:** Получить отзывы для книги с ID=1:
- `book`: 1

### POST /api/reviews/ - Создать отзыв

**Требует авторизации**

1. Убедитесь, что вы авторизованы
2. Найдите эндпоинт **POST /api/reviews/**
3. Нажмите "Try it out"
4. Введите данные:
   ```json
   {
     "book_id": 1,
     "rating": 5,
     "comment": "Отличная книга! Очень рекомендую."
   }
   ```
5. Нажмите "Execute"

**Примечание:** Один пользователь может оставить только один отзыв на книгу.

---

## 📊 Экспорт данных (только для администраторов)

### GET /api/export/ - Экспорт в XLSX

**Требует авторизации администратора**

1. Войдите как администратор (admin / admin123)
2. Авторизуйтесь с admin токеном
3. Найдите эндпоинт **GET /api/export/**
4. Нажмите "Try it out"
5. Заполните параметры:
   - `model` - имя модели (book, author, order, review, user)
   - `fields` - поля для экспорта (можно добавить несколько)

**Пример 1:** Экспорт книг
- `model`: book
- `fields`: title
- `fields`: price
- `fields`: stock

**Пример 2:** Экспорт пользователей
- `model`: user
- `fields`: username
- `fields`: email
- `fields`: role
- `fields`: created_at

6. Нажмите "Execute"
7. Нажмите "Download" чтобы скачать Excel файл

---

## 🎯 Полный сценарий тестирования

### Сценарий 1: Новый пользователь оформляет заказ

1. **Регистрация**
   - POST /api/register/
   - Сохраните токен

2. **Авторизация в Swagger**
   - Кнопка "Authorize"
   - Введите токен

3. **Просмотр каталога**
   - GET /api/books/
   - Найдите книги

4. **Просмотр деталей книги**
   - GET /api/books/1/
   - Посмотрите информацию

5. **Создание заказа**
   - POST /api/create-order/
   - Укажите книги и количество

6. **Просмотр заказов**
   - GET /api/orders/
   - Увидите созданный заказ

7. **Оставить отзыв**
   - POST /api/reviews/
   - Оцените книгу

### Сценарий 2: Администратор управляет данными

1. **Вход как администратор**
   - POST /api/login/
   - username: admin, password: admin123

2. **Авторизация**
   - Кнопка "Authorize"
   - Введите admin токен

3. **Создание автора**
   - POST /api/authors/
   - Добавьте нового автора

4. **Создание книги**
   - POST /api/books/
   - Добавьте новую книгу

5. **Просмотр всех заказов**
   - GET /api/orders/
   - Администратор видит все заказы

6. **Экспорт данных**
   - GET /api/export/?model=book&fields=title&fields=price
   - Скачайте отчет

---

## 🔍 Проверка требований через Swagger

### ✅ RESTful API с CRUD
- **Create:** POST /api/books/, POST /api/authors/, POST /api/reviews/
- **Read:** GET /api/books/, GET /api/books/{id}/, и т.д.
- **Update:** PUT /api/books/{id}/
- **Delete:** DELETE /api/books/{id}/

### ✅ JWT Аутентификация
- Получение токена: POST /api/login/, POST /api/register/
- Использование: Кнопка "Authorize" в Swagger

### ✅ Пагинация
- Любой GET запрос со списком возвращает:
  ```json
  {
    "count": 100,
    "next": "http://localhost:8000/api/books/?page=2",
    "previous": null,
    "results": [...]
  }
  ```

### ✅ Фильтрация и поиск
- GET /api/books/?search=война
- GET /api/books/?author=1
- GET /api/books/?ordering=price
- GET /api/reviews/?book=1

### ✅ Swagger с кнопкой Authorize
- Кнопка "Authorize" присутствует (замок справа вверху)
- Можно вводить JWT токен
- Можно делать авторизованные запросы

---

## 🐛 Решение проблем

### Ошибка 401 Unauthorized
**Причина:** Не авторизованы или токен истек

**Решение:**
1. Получите новый токен через POST /api/login/
2. Нажмите "Authorize"
3. Введите `Bearer <новый_токен>`
4. Попробуйте снова

### Ошибка 403 Forbidden
**Причина:** Недостаточно прав (например, не администратор)

**Решение:**
- Для экспорта данных войдите как администратор (admin / admin123)

### Ошибка 400 Bad Request
**Причина:** Неверные данные в запросе

**Решение:**
- Проверьте формат JSON
- Убедитесь, что все обязательные поля заполнены
- Посмотрите детали ошибки в ответе

### Токен не принимается
**Причина:** Неверный формат

**Правильный формат:**
```
Bearer eyJ0eXAiOiJKV1QiLCJhbGc...
```

**Неправильные форматы:**
```
eyJ0eXAiOiJKV1QiLCJhbGc...        (нет Bearer)
Bearer: eyJ0eXAiOiJKV1QiLCJhbGc...  (лишнее двоеточие)
```

---

## 💡 Полезные советы

1. **Копирование токена:**
   - Выделите токен двойным кликом
   - Ctrl+C для копирования
   - Убедитесь, что скопировали полностью (токены длинные!)

2. **Сохранение токена:**
   - Сохраните токен в текстовом файле для удобства
   - Токен действителен 1 час

3. **Тестирование пагинации:**
   - Используйте параметр `page=2`, `page=3` и т.д.
   - Проверьте поля `next` и `previous` в ответе

4. **Тестирование фильтров:**
   - Комбинируйте параметры: `?search=война&ordering=-price`

5. **Просмотр схемы:**
   - В Swagger каждый эндпоинт показывает схему запроса и ответа
   - Нажмите "Schema" чтобы увидеть структуру данных

---

## 📞 Дополнительная информация

- **Полная документация:** [README.md](README.md)
- **Быстрый старт:** [QUICKSTART.md](QUICKSTART.md)
- **Проверка требований:** [REQUIREMENTS_CHECKLIST.md](REQUIREMENTS_CHECKLIST.md)

---

**Приятного тестирования! 📚✨**