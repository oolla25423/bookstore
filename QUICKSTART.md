# 🚀 Быстрый старт - Книжный магазин

## Запуск проекта за 5 минут

### Шаг 1: Клонирование и запуск

```bash
# Клонируйте репозиторий (если еще не сделано)
cd C:\Programming\bookstore

# Запустите все сервисы через Docker Compose
docker-compose up --build
```

Подождите, пока все контейнеры запустятся (это может занять 1-2 минуты при первом запуске).

### Шаг 2: Создание базы данных

В **новом терминале** выполните:

```bash
# Примените миграции
docker-compose exec web python manage.py migrate

# Создайте суперпользователя
docker-compose exec web python manage.py createsuperuser
# Введите: username: admin, email: admin@bookstore.com, password: admin123

# (Опционально) Загрузите тестовые данные
docker-compose exec web python manage.py shell < create_test_data.py
```

### Шаг 3: Откройте приложение

**Frontend (главная страница):**
🌐 http://localhost:3001

**Backend API:**
🔧 http://localhost:8000/api/

**Swagger документация:**
📚 http://localhost:8000/api/schema/swagger-ui/

**Админ-панель:**
👨‍💼 http://localhost:8000/admin/
- Логин: admin
- Пароль: admin123

---

## 🎯 Что можно сделать сразу

### 1. Просмотр каталога (Гостевой режим)
- Откройте http://localhost:3001
- Посмотрите каталог книг
- Используйте поиск и фильтры
- Посмотрите авторов

### 2. Регистрация и вход
- Нажмите "Регистрация" в правом верхнем углу
- Заполните форму
- Войдите в систему

### 3. Оформление заказа
- Войдите в систему
- Выберите книгу из каталога
- Нажмите "Добавить в корзину"
- Перейдите в корзину
- Нажмите "Оформить заказ"

### 4. Оставьте отзыв
- Откройте страницу книги
- Прокрутите вниз к форме отзыва
- Выберите оценку и напишите комментарий
- Отправьте отзыв

### 5. Работа с API через Swagger
- Откройте http://localhost:8000/api/schema/swagger-ui/
- Попробуйте GET запросы (без авторизации)
- Для POST запросов:
  1. Сначала войдите через `/api/login/`
  2. Скопируйте `access` токен из ответа
  3. Нажмите кнопку "Authorize" в Swagger
  4. Введите: `Bearer <ваш_токен>`
  5. Теперь можно делать авторизованные запросы

### 6. Админ-панель
- Откройте http://localhost:8000/admin/
- Войдите (admin / admin123)
- Управляйте пользователями, книгами, заказами
- Экспортируйте данные через API: `/api/export/?model=book&fields=title&fields=price`

---

## 📱 Тестирование на разных устройствах

### Desktop
Откройте в обычном браузере: http://localhost:3001

### Mobile (симуляция)
1. Откройте DevTools (F12)
2. Включите режим адаптивного дизайна (Ctrl+Shift+M)
3. Выберите устройство (iPhone, iPad и т.д.)

### Реальное мобильное устройство
1. Узнайте локальный IP вашего компьютера:
   ```bash
   # Windows
   ipconfig
   # Найдите IPv4 адрес (например, 192.168.1.100)
   ```

2. В `bookstore/settings.py` добавьте ваш IP в `ALLOWED_HOSTS`:
   ```python
   ALLOWED_HOSTS = ['localhost', '127.0.0.1', '0.0.0.0', '192.168.1.100']
   ```

3. В `frontend/app.js` измените `API_BASE`:
   ```javascript
   const API_BASE = 'http://192.168.1.100:8000/api';
   ```

4. Перезапустите контейнеры:
   ```bash
   docker-compose restart
   ```

5. На мобильном устройстве откройте: `http://192.168.1.100:3001`

---

## 🧪 Тестовые данные

Если вы запустили скрипт `create_test_data.py`, у вас есть:

### Тестовые пользователи:
| Логин | Пароль   | Роль          |
|-------|----------|---------------|
| admin | admin123 | Администратор |
| user1 | user1234 | Пользователь  |
| user2 | user1234 | Пользователь  |
| user3 | user1234 | Пользователь  |

### Данные:
- 8 авторов (Толстой, Достоевский, Пушкин, и др.)
- 16 книг (классика русской литературы)
- 4 заказа
- 12 отзывов

---

## 🔍 Примеры API запросов

### Регистрация
```bash
curl -X POST http://localhost:8000/api/register/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "newuser",
    "email": "newuser@example.com",
    "password": "password123",
    "password_confirm": "password123"
  }'
```

### Вход
```bash
curl -X POST http://localhost:8000/api/login/ \
  -H "Content-Type: application/json" \
  -d '{
    "username": "user1",
    "password": "user1234"
  }'
```

### Получение списка книг
```bash
curl http://localhost:8000/api/books/
```

### Поиск книг
```bash
curl "http://localhost:8000/api/books/?search=война"
```

### Создание заказа (требуется токен)
```bash
curl -X POST http://localhost:8000/api/create-order/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <ваш_токен>" \
  -d '{
    "items": [
      {"book_id": 1, "quantity": 2},
      {"book_id": 3, "quantity": 1}
    ]
  }'
```

### Экспорт данных (только для админа)
```bash
curl "http://localhost:8000/api/export/?model=book&fields=title&fields=price&fields=stock" \
  -H "Authorization: Bearer <admin_токен>" \
  --output books.xlsx
```

---

## 🛑 Остановка проекта

```bash
# Остановить контейнеры (данные сохраняются)
docker-compose stop

# Остановить и удалить контейнеры (данные сохраняются в volume)
docker-compose down

# Остановить и удалить ВСЁ включая данные БД
docker-compose down -v
```

---

## 🔄 Перезапуск после изменений

### Изменения в Python коде:
```bash
docker-compose restart web
```

### Изменения в Frontend:
Просто обновите страницу в браузере (Ctrl+F5)

### Изменения в зависимостях или Docker конфигурации:
```bash
docker-compose down
docker-compose up --build
```

---

## ❓ Частые проблемы

### Порт уже занят
```bash
# Проверьте, что занимает порт
netstat -ano | findstr :8000

# Остановите процесс или измените порт в docker-compose.yml
```

### База данных не создана
```bash
# Пересоздайте контейнеры
docker-compose down -v
docker-compose up --build

# Затем снова примените миграции
docker-compose exec web python manage.py migrate
```

### CORS ошибки
Убедитесь, что в `settings.py` есть:
```python
CORS_ALLOWED_ORIGINS = [
    "http://localhost:3001",
    "http://127.0.0.1:3001",
]
```

### Frontend не загружает данные
Проверьте в консоли браузера (F12) какие ошибки возникают.
Убедитесь, что backend запущен и доступен по http://localhost:8000

---

## 📖 Дополнительная информация

Полная документация: [README.md](README.md)

## ✅ Чеклист готовности

- [ ] Docker и Docker Compose установлены
- [ ] Выполнено `docker-compose up --build`
- [ ] Применены миграции
- [ ] Создан суперпользователь
- [ ] Frontend открывается на http://localhost:3001
- [ ] Backend API доступен на http://localhost:8000/api/
- [ ] Swagger работает на http://localhost:8000/api/schema/swagger-ui/
- [ ] Админ-панель доступна на http://localhost:8000/admin/
- [ ] Можно зарегистрироваться и войти
- [ ] Можно создать заказ
- [ ] Можно оставить отзыв

Если все пункты отмечены - поздравляем! 🎉 Проект готов к использованию!

---

**Приятной работы! 📚✨**